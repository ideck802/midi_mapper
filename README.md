This is a MIDI mapping tool written in python.

Input options:
- Button
- Key
- Knob

Output options:
- Keypress to window
- Type a string to a window
- Change a MIDI button light
- Send an OSC message
